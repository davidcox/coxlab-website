----- 
:title: "Tag: computation"
:type: page
:filters_pre: 
- erb
:feed: /tags/computation/
:feed_title: Tag 'computation'
:permalink: computation
-----

<p>4 items are tagged with <em>computation</em>:</p>
<ul class="tag_results">
	<% articles_tagged_with('computation').each do |a| %>
		<%= render 'tagged_item', :tagged_item => a %>
	<% end %>
</ul>
		
